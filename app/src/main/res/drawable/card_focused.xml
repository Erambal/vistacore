<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/card_background_elevated" />
    <corners android:radius="@dimen/card_corner_radius" />
    <!-- Thicker, unmistakable "you are here" border so the cursor is obvious
         from across the room — matches the home_tile focus used elsewhere. -->
    <stroke
        android:width="6dp"
        android:color="@color/focus_ring" />
</shape>
