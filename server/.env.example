# API key for authenticating requests from TV boxes
VISTAFILTER_API_KEY=changeme

# Whisper model size: tiny, base, small, medium, large-v3
# base = good balance of speed/accuracy for profanity detection
# small = better accuracy, ~2x slower
# medium = best accuracy, ~4x slower (needs 4GB+ RAM)
WHISPER_MODEL=base
